%ul
  - @groups.each do |group|
    %li.side-group
      = link_to "#{group.group_name}", group_messages_path(group.id), class: "side-group__name"
      - if group.messages == []
        %p.side-group__chat メッセージは存在しません。
      - else
        %p.side-group__chat #{group.messages.last.body}
