.side
  .side-header
    = render "side-header"
  .side-groups
    = render "side-groups"
.main
  - if params[:group_id] != nil
    .main-header.clearfix
      = render "main-header"
    .main-body
      = render "main-body"
    .main-footer.clearfix
      = render "main-footer"
